version: "3.9"
services:
  ai_interview_app:
    build: .
    container_name: ai_interview_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
